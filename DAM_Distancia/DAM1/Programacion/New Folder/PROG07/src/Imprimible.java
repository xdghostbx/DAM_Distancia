
/**
 * Definimos una interfaz imprimible con un unico método. Este método lo que hace es que las 
 * clases que utilizen esta clase, deben usar si o si un método "devolverInfoToString()"
 * 
 */
public interface Imprimible {
    public String devolverInfoString();
}
