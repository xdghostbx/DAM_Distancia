Introducción: Aplicacicones, Ejecutables y Procesos
    Aplicacion: Es un tipo de programa informatico diseñado como una herramienta para resolver de forma automatica un problema especifico del usuarioç

    Ejecutable: Es un fichero qwue contiene el codigo binario o interpretado que será ejecutado por el ordenador

    Proceso: es el programa en ejecucion

    Tipos de Ejecutables:
        Binarios
        Interpretados
        Librerías

Gestion de procesos:
    Tipos de procesos:
        -Por lotes: Formados por una serie de tareas de las que el usuario solo le interesa el resultado final.
        -Interactivos: El proceso interactua continuamente con el usuario y actua de acuerdo a las acciones o datos que se le suministra.
        -Tiempo real: Tareas en las que es critico el tiempo de respuesta del sistema.
    
    Gestion de procesos. Introduccion
        Estados de un proceso:
            1.- Los procesos nuevos entran en la cola de procesos activos del sistema
            2.- Los procesos van avanzando posiciones en la cola de procesos activos hasta que les toca su turno para que el SO les conceda uso de CPU
            3.- El SO concede uso de CPU a cada proceso durante un tiempo determinado y equitativo a cada proceso, que se denomina quantum. Cuando el proceso consume su quantum, se pausa y se envia al final de la cola
            4.- Si un proceso se finaliza, sale del sistema de gestión de procesos

        Situaciones características: 
            Cuando un proceso necesita datos de un archivo o entrada de datos, como son mas lentos que la CPU, se pausan esperando la entrada/salida y los otros procesos avanzan hasta que se recibe la E/S. Cuando se recibe, la CPU pasa el proceso a activo para ejecutar sus instrucciones
            Tambien existen procesos del sistema que tienen prioridad antes los procesos de usuario. Estos siempre se ejecutarán antes que a los del usuario

        Estados de ciclo de vida de un proceso:
            Nuevo
            Listo
            En ejecucion
            Bloqueado
            Suspendido
            Terminado
        
    Planificacion de procesos por el Sistema Operativo